# host.toml for unit tests — unrelated to any real deployment.
[model_limits]
max_train_param_b = 7
max_train_seq_length = 1024
max_train_batch_size = 4

[concurrency]
max_concurrent_train_jobs = 1
max_concurrent_kbd_jobs = 1

[paths]
storage_base = "/tmp/koni_test_storage"
