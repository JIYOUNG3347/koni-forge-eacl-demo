# KONI-Forge Lite dependencies.
#
# Ranges rather than exact pins, so the same file installs on macOS (Apple
# Silicon, MPS), Linux with CUDA, and CPU-only hosts. Versions verified on
# Python 3.11 to 3.13.

# ── Web framework ────────────────────────────────────────
fastapi>=0.115,<0.116
uvicorn[standard]>=0.34,<0.35
python-multipart>=0.0.20
pydantic>=2.10,<3

# ── Task queue and cache ─────────────────────────────────
celery[redis]>=5.4,<6
redis>=5.2,<6

# ── Database ─────────────────────────────────────────────
sqlalchemy>=2.0,<3
aiosqlite>=0.20
greenlet>=3.0          # required by SQLAlchemy's async engine

# ── LLM training ─────────────────────────────────────────
# trl is capped below 1.7: its chunked cross-entropy path reads
# `outputs.last_hidden_state`, which breaks SFT training. transformers is
# capped below 5 because trl 1.6 does not support the 5.x API.
torch>=2.6
transformers>=4.49,<5
datasets>=3.6,<5
accelerate>=1.6
peft>=0.15
trl>=1.5,<1.7
tensorboard>=2.19

# ── Document processing ──────────────────────────────────
pdfplumber>=0.11
python-docx>=1.1
python-pptx>=1.0
numpy>=1.26

# ── LLM clients ──────────────────────────────────────────
openai>=3.16.2
huggingface_hub>=0.30,<1
httpx>=0.28

# ── RAG ──────────────────────────────────────────────────
# chromadb 1.x is required for numpy 2 compatibility;
# sentence-transformers is capped below 6, which needs transformers 5.
chromadb>=1.0,<2
sentence-transformers>=3.0,<6

# ── Security ─────────────────────────────────────────────
bcrypt>=4.2
python-magic>=0.4.27

# ── Utilities ────────────────────────────────────────────
tomli>=2.0; python_version < "3.11"   # tomllib is stdlib from 3.11

# ── Development ──────────────────────────────────────────
pytest>=8.3
pytest-asyncio>=0.24
fakeredis>=2.26
ruff>=0.4
